"""Sessions domain: session and message lifecycle.

Also serves the artifact HTTP surface for its sessions (list/retrieve/
download) per the MVP simplification in REPOSITORY_STRUCTURE.md — the
`artifacts` domain has no router of its own.
"""
