"""Per-content-type formatters for the Ship30 skill.

Each formatter takes a raw model completion and enforces platform
constraints (PRD §6.5 acceptance criteria: "respects platform constraints").
"""
