"""Artifact skill: direct artifact-producing operations invoked via the
router, independent of QA/Research (e.g. explicit "turn this into a
document") — distinct from the `artifacts` domain's persistence/CRUD.
"""
