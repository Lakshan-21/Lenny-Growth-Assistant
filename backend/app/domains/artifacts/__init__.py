"""Artifacts domain: persistence, rendering (Markdown/HTML), copy/download
support for artifacts attached to sessions.

No HTTP router in MVP — exposed via `sessions/router.py` instead (see
REPOSITORY_STRUCTURE.md's MVP-simplification notes).
"""
